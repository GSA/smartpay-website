---
const { text, link } = Astro.props;
const isInternal = link.includes("http")? false: true
---
{isInternal? <a href={`/${link}`} class="usa-button">{text}</a>:<a href={link} class="usa-button usa-link--external">{text}</a>}



